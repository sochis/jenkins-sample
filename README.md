# jenkins-sample by using docker

- Using jenkins user in docker container for security.
- Uncomment volume of docker-compose file if updating jenkins after setting jenkins.war on docker/update folder.
